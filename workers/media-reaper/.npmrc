# sharp ships with miniflare for local `wrangler dev` image handling, and its
# install script fails on Node 20 by trying to build from source. It is not
# needed to deploy this Worker, so no install script runs here.
ignore-scripts=true
